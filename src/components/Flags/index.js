import React, { PureComponent } from 'react';
import { Grid } from "../../components/Layout/index";
import { theme } from "../../theme";

export class Flags extends PureComponent {
    render() {

        return (
            <Grid/>
        );
    }
}
